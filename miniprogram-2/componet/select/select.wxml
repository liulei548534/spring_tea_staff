<!--componet/select/select.wxml-->
<!--animation="{{animationData}}"   下箭头的动画效果
 data-index="{{index}}"   当前元素被点击时的索引
 selectToggle  模仿下拉选项框隐藏和显示的事件
 setText  模仿下拉选项框选择子项之后，设置内容的事件
 selectShow  表示option选项显示与否-->
<!-- <view class='com-selectBox'>
    <view class='com-sContent' bindtap='selectToggle'>
        <view class='com-sTxt'>{{nowText}}</view>
        <image src='../../images/lower_l.png'  class='com-sImg'  animation="{{animationData}}"></image>
    </view>
    <view class='com-sList' wx:if="{{selectShow}}">
        <view wx:for="{{propArray}}" data-index="{{index}}" wx:key='' class='com-sItem' bindtap='setText'>{{item.text}}</view>
    </view>
</view> -->
<view class="search_start">
    <navigator class="search_na" url="../../pages/warehouse/warehouse" open-type="navigate">
        搜索
    </navigator>
</view>